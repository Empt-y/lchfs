[package]
name = "lchfs-compress"
description = "Adaptive entropy-sampled zstd compression for LCHFS"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
zstd = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
criterion = { workspace = true }

[[bench]]
name = "compression"
harness = false
